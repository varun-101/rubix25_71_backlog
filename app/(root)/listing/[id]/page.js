import React from "react";
import { formatDate } from "@/lib/utils";
const page = ({params}) => {
    console.log(params);
    
    return (
        <div>
            <h1>Listing</h1>
        </div>
    )
}

export default page;